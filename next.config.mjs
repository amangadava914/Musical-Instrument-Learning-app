/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "encrypted-tbn0.gstatic.com",
      "pbs.twimg.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
